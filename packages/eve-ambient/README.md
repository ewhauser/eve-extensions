# eve-ambient has moved

`eve-ambient` now lives in the standalone
[`ewhauser/eve-ambient`](https://github.com/ewhauser/eve-ambient) repository and
is published on npm as
[`@ewhauser/eve-ambient`](https://www.npmjs.com/package/@ewhauser/eve-ambient).

```sh
pnpm add @ewhauser/eve-ambient
```

Replace imports from `eve-ambient` with `@ewhauser/eve-ambient`. The old source,
tests, package manifest, and release automation have been removed from this
repository; this file remains only as a migration pointer.
